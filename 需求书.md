# LanDrop 项目需求书

| | |
|---|---|
| 文档版本 | v2.0 |
| 对应代码版本 | 架构迁移完成(阶段 A–H 全部完成,尚未打新 release 版本号) |
| 更新日期 | 2026-08-03 |
| 仓库 | https://github.com/DavidRedVest/LanDrop |

> 说明:本项目从"需求 → 实现"是一边做一边细化的过程,并非先有完整需求书再开发。v1.0 版本写于架构迁移(自定义协议 → 标准 FTP)进行到一半的时候,当时客户端/服务端 GUI 实际连接的还是旧协议;这个 v2.0 版本是在迁移**全部完成**(GUI 已接入新核心、旧协议代码已删除)之后的更新,反映的是当前的最终状态。

---

## 1. 项目概述

### 1.1 项目名称

**LanDrop**(代码仓库/构建产物历史上叫过 `FTP_Soft`,现在只有品牌名改了,可执行文件、窗口标题、配置项都已统一为 LanDrop)。

### 1.2 一句话定位

一个跨平台(macOS / Windows / Linux)、客户端 + 服务端一体的局域网文件传输工具,底层是标准 FTP 协议(RFC 959),界面参考 FileZilla / UltraFTP 的双栏浏览 + 传输队列模式,用 Qt6 + C++17 编写,核心网络逻辑完全不依赖 Qt。

### 1.3 要解决的问题

用户在自己的多台设备之间(典型场景:一台 Mac + 一台 Windows 笔记本)传文件时,常见的选项都不够好:

- U 盘拷贝:慢,还要来回插拔;
- 第三方网盘/云同步:依赖公网、大文件慢、隐私顾虑、不稳定;
- 系统自带的隔空投送 / 网络共享:平台绑定(如 AirDrop 只能苹果生态内)、配置繁琐、大文件容易断。

LanDrop 的目标是:**在同一局域网内,双击打开就能用,大文件传输稳定不掉线,进度可视化,不需要额外账号或公网依赖;同时因为走的是标准 FTP,也能连接任意第三方 FTP 服务器(比如 NAS),不局限于两台都装了 LanDrop 的设备。**

### 1.4 目标用户

- 拥有多台设备(尤其是跨平台,如 Mac + Windows)的个人开发者/技术爱好者;
- 需要稳定传输较大文件(GB 级)且希望有可视化进度、断点续传的场景;
- 需要用同一个客户端连接局域网内其它标准 FTP 服务(NAS 等)的场景;
- 不需要企业级多用户权限体系,只需要基本的用户名/密码认证。

---

## 2. 项目背景与发展历程

项目最初以 `FTP_Soft` 为名启动,定位是"仿 FileZilla/UltraFTP 的局域网文件传输工具"。完整发展脉络:

1. **初版实现**:基于 Qt(`QTcpSocket`)的自定义二进制协议(非标准 FTP),完成局域网内两台自有设备间的文件互传,包含断点续传、失败重试、完整性校验。
2. **品牌化**:项目更名为 LanDrop,窗口标题、配置项、图标全部统一。
3. **发布工程化**:接入 GitHub Actions,实现 macOS/Windows/Linux 三平台自动构建 + GitHub Release 发布,解决了 macOS Gatekeeper 拦截、代码签名等实际分发问题。
4. **真实使用发现的 Bug 修复**:大文件(1.7GB+)传输中途断线、进度条渲染错位(两次,根因不同)、站点管理器连错站点等问题,均通过真实场景复现后修复。
5. **架构重构(已完成)**:应用户明确要求,把"Qt 耦合的自定义协议"重写为"核心逻辑(网络/并发)与 UI 彻底分离、协议改为标准 FTP(RFC 959)"的新架构。分八个阶段(A–H)推进,每阶段独立编译验证,最终把 GUI 完全接入新核心并删除了旧协议代码。
6. **架构迁移完成后的功能补完(已完成)**:在新架构基础上补上了两项当初讨论过但延后的能力——客户端并发传输连接池(标准 FTP 协议本身的限制导致不能照搬旧协议的并发方式,需要新设计)、局域网自动发现设备(自定义 UDP 广播,不是 mDNS)。SFTP 客户端支持经过讨论后**明确排除**,不在计划内(引入 SSH/加密库的工程量和收益不成比例,详见第 9.4 节)。

---

## 3. 总体目标与设计原则

| 目标 | 说明 |
|---|---|
| 稳定优先 | 大文件、长时间传输不能掉线;所有关键 bug 必须用真实场景复现后再修,不能只凭代码走查 |
| 跨平台一致 | macOS / Windows / Linux 三端功能、界面、操作方式一致 |
| 开箱即用 | 发布物是解压即用的可执行文件,不需要安装包、不需要额外装 Qt(macOS/Windows 已打包运行库) |
| 核心与界面解耦 | 网络通信、并发模型用标准 C++ + 原生 OS API 实现,不依赖 Qt(`core/` 目录);Qt 只负责界面(`client/`、`server/`) |
| 协议标准化 | 标准 FTP(RFC 959),具备连接第三方 FTP 服务器的能力,不再是自造协议 |
| 安全默认值 | 密码加盐哈希存储、路径穿越防护、未配置认证时默认拒绝而不是放行 |

---

## 4. 系统架构

### 4.1 分层结构

```
┌───────────────────────────────────────────────────────────┐
│  GUI 层(client/、server/)—— 只在这里出现 Qt                │
│  MainWindow / FileBrowserPanel / TransferWidget /          │
│  SiteManagerDialog / ServerWindow                           │
│  Connection / TransferQueue / FTPServer                     │
│  —— 均为薄 Qt 包装层,内部委托给下面的 core/                 │
│  —— 通过 QMetaObject::invokeMethod(..., QueuedConnection)   │
│     把 core/ 的 std::function 回调安全地转发到 UI 线程       │
└───────────────────────────────────────────────────────────┘
                            │
┌───────────────────────────────────────────────────────────┐
│  核心层(core/)—— 完全不依赖 Qt,只用标准 C++ + 原生 OS API   │
│  core/platform/socket.*, udp_socket.*  — 原生 TCP/UDP socket │
│  core/heartbeat.*                      — 协议无关心跳线程    │
│  core/hash/sha256.*                    — 自实现 SHA-256      │
│  core/ftp/ftp_client.*                 — RFC 959 客户端状态机 │
│  core/ftp/ftp_server.* + ftp_session.* — RFC 959 服务端       │
│  core/ftp/ftp_paths.*                  — 路径安全             │
│  core/ftp/ftp_transfer_manager.*       — 客户端并发传输池      │
│  core/discovery/lan_discovery.*        — 局域网自动发现        │
└───────────────────────────────────────────────────────────┘
```

`common/` 只剩下协议无关的通用工具(`common/utils.h/.cpp`:密码哈希、文件哈希、人类可读格式化)和 GUI 层仍需要的两个小类型(`common/ftptypes.h`:`FTP::FileInfo`、`FTP::DEFAULT_PORT`)。

### 4.2 Qt 侧关键类

- `Connection`(`client/connection.h/.cpp`)—— 薄包装,内部一条 `std::thread` 跑串行任务队列,驱动一个线程私有的 `core::FtpClient`。`login`/`listDirectory`/`mkdir`/`rmdir`/`deleteFile`/`rename` 每个方法把一个任务推进队列,结果通过 Qt 信号(跨线程用 `QMetaObject::invokeMethod`)返回。
- `TransferQueue`(`client/transfer.h/.cpp`,`QAbstractTableModel`)—— 薄包装,内部持有一个 `core::FtpTransferManager`(登录成功后由 `MainWindow::onLoginResult()` 调用 `setConnectionInfo()` 创建)。进度回调在跨线程转发前先做 ~200ms 节流,避免高频事件灌爆 Qt 事件队列。
- `FTPServer`(`server/ftpserver.h/.cpp`)—— 薄包装,内部持有一个 `core::FtpServer`,不再继承 `QTcpServer`(核心自己在原生 socket 上做 accept)。密码哈希表用 `std::mutex` 保护(核心的鉴权回调从任意会话后台线程并发调用)。

### 4.3 核心层(`core/`)关键类

- `core::FtpClient` —— 同步/阻塞式 RFC 959 客户端状态机,调用方自己负责起线程跑它。
- `core::FtpServer` / `core::FtpSession` —— accept 线程 + 每客户端一条 `std::thread` 的服务端。
- `core::FtpTransferManager` —— 客户端并发传输池:固定大小(默认 3)的 worker 池,每个 worker 是一条常驻登录的 `core::FtpClient` + 专属线程,循环处理任务队列。标准 FTP 一条控制连接同一时刻只能做一件事,不能像旧协议那样共用一条控制通道申请多个并发传输令牌,所以并发传输必须是多条独立 FTP 会话——这是 FileZilla 等主流多连接 FTP 客户端的标准做法。
- `core::DiscoveryBeacon` / `core::DiscoveryListener` —— 自定义 UDP 广播局域网发现(端口 52121,一行文本 `LANDROP1|设备名|服务端口`),不是 mDNS/DNS-SD。

---

## 5. 功能需求(现状:全部已实现)

### 5.1 客户端功能

| 功能 | 说明 |
|---|---|
| 连接管理 | 站点管理器保存主机/端口/用户名/密码,一键连接;工具栏可见的"连接"/"断开连接"按钮 |
| 局域网发现 | 站点管理对话框内置"局域网发现"面板,自动监听局域网内正在广播的 LanDrop 服务端,双击填入地址 |
| 双栏文件浏览 | 左本地、右远程,同一套控件和交互:导航目录、新建文件夹、重命名、删除 |
| 文件上传/下载 | 选中文件后可发起上传到远程当前目录 / 下载到本地当前目录 |
| 并发传输 | 默认最多 3 个文件同时传输(每个占用一条独立 FTP 会话) |
| 传输队列可视化 | 进度条、传输速度、剩余时间(ETA)、状态(排队中/连接中/传输中/已完成/失败/已暂停/已取消) |
| 传输控制 | 暂停 / 继续 / 取消 / 从队列删除 / 清除已完成 |
| 断点续传 | 传输中断后重新发起,自动从已传输字节位置续传(`REST` 命令) |
| 失败自动重试 | 网络抖动导致失败会自动线性退避重试,超过次数上限后停止 |
| 记住密码(可选) | 明文保存在本机 QSettings,UI 上明确提示"仅建议个人设备使用" |

### 5.2 服务端功能

| 功能 | 说明 |
|---|---|
| 服务配置 | 指定根目录、控制端口(数据端口由 `PASV` 动态分配,不再是固定的"端口+1") |
| 局域网可发现 | "允许被局域网自动发现"开关,勾选后周期广播,状态持久化 |
| 用户管理 | 添加/删除用户(用户名+密码),密码加盐哈希存储,不落地明文 |
| 会话可视化 | 实时显示当前连接会话数、连接/断开日志、传输开始/完成日志 |
| 路径安全 | 所有客户端请求路径经过校验和钳制,禁止目录穿越到根目录之外 |

### 5.3 传输引擎功能需求

| 需求 | 说明 |
|---|---|
| 大文件稳定性 | GB 级文件传输全程不因 UI 刷新或心跳超时误断线(旧协议时代曾用 1.8GB 实测验证;新协议栈迁移后用 1.5GB 一次性手动验证复测,~34 秒完成,无掉线,细节见第 10 节) |
| 分块传输 | 数据读写用固定 256KB 内存块,不整文件读入内存 |
| 心跳保活 | 空闲/浏览窗口内,控制通道独立心跳线程周期发送 `NOOP` 防止被 NAT/防火墙判定超时;传输进行中暂停心跳(RFC 959 控制通道在 `RETR`/`STOR` 期间被占用,插入 `NOOP` 会打乱回复流,数据通道本身在传输期间会保持连接活跃,不存在超时风险) |
| 标准 FTP 兼容 | 可连接任意标准 FTP 服务器,不局限于 LanDrop 自己的服务端(受限于第 7.2 节列出的已知规格差异) |

### 5.4 跨平台打包与分发需求

| 需求 | 说明 |
|---|---|
| 三平台构建 | macOS(`.app` + `macdeployqt`)、Windows(`windeployqt`)、Linux 二进制,均由 GitHub Actions 自动构建 |
| 免安装分发 | 解压即用,不需要安装向导,不额外写入系统 |
| 自动发布 | 推送 `v*.*.*` 格式的 tag 自动触发构建并创建 GitHub Release,附带各平台压缩包 |
| Gatekeeper/SmartScreen 说明 | 由于没有付费代码签名证书,README 中需明确写清楚绕过步骤 |

### 5.5 安全需求

| 需求 | 说明 |
|---|---|
| 密码存储 | 服务端密码加盐 SHA-256 哈希存储,任何时候不比较/存储明文 |
| 路径穿越防护 | 所有客户端提交的路径必须经过规范化+根目录钳制校验,含 `..`-逃逸和字符串前缀边界(如 `/root_evil` vs `/root`)攻击的回归测试 |
| 认证默认拒绝 | 未配置认证回调时,任何登录一律拒绝,而不是默认放行 |

### 5.6 明确排除的功能

| 功能 | 排除原因 |
|---|---|
| SFTP 客户端支持 | 需要引入 SSH/加密库(如 libssh2 + OpenSSL/mbedTLS),三平台 CI 集成成本高;只能做"客户端连别人的 SFTP 服务器",无法让 LanDrop 自己变成 SFTP 服务端。用户明确要求跳过,优先做完 GUI 接入和局域网发现 |
| 标准 mDNS/DNS-SD 局域网发现 | 实际需求只是"LanDrop 找 LanDrop",手写一套 DNS 报文编解码器的代价和收益不成比例,改用自定义 UDP 广播协议 |

---

## 6. 非功能需求

| 类别 | 需求 |
|---|---|
| 性能 | 局域网内大文件传输速度应接近网卡/磁盘 IO 上限 |
| 稳定性 | 长时间/大文件传输不能因 UI 层原因(如高频重绘、高频跨线程事件)拖慢主线程导致心跳误超时——客户端和服务端两侧都做了节流,详见第 9.3 节 |
| 兼容性 | macOS(Intel/Apple Silicon)、Windows、Linux 三端一致行为 |
| 可维护性 | 核心网络逻辑与 UI 逻辑严格分离,`core/` 可独立编译测试,不依赖 Qt |
| 可测试性 | 关键逻辑能在无 GUI 环境下自动化验证,见第 10 节 |
| 安全性 | 密码不落地明文;路径访问严格限制在授权根目录内 |

---

## 7. 协议设计

### 7.1 标准 FTP(RFC 959)

- 命令集:`USER`/`PASS`/`PWD`/`XPWD`/`CWD`/`XCWD`/`CDUP`/`XCUP`/`TYPE`/`PASV`/`LIST`/`MLSD`/`RETR`/`STOR`/`REST`/`DELE`/`MKD`/`XMKD`/`RMD`/`XRMD`/`RNFR`+`RNTO`/`NOOP`/`FEAT`/`SYST`/`QUIT`。
- 目录列表优先用 `MLSD`(机器可读,通过 `FEAT` 探测服务端是否支持),不支持时退回解析 `LIST` 的类 Unix `ls -l` 文本格式(没有严格标准,尽力而为)。
- 数据通道走 `PASV` 被动模式;服务端每次 `PASV` 动态分配一个临时端口(不是固定的"控制端口+1"),一次性使用,每次传输前客户端必须重新 `PASV`。
- 续传用 `REST <offset>` + 紧跟的 `RETR`/`STOR`。

### 7.2 已知规格差异(相对旧自定义协议,标准化后的必然取舍,不是遗漏)

| 差异 | 说明 |
|---|---|
| 无内建完整性校验 | 标准 FTP 没有"传输后自动核对哈希"的机制。对第三方服务器没有办法;对 LanDrop 自己的服务端,未来可以加一个通过 `FEAT` 探测的非标准扩展命令,当前**未实现** |
| 远程文件浏览的修改时间/权限列为空 | `core::FtpClient` 目前不解析 MLSD 的 `Modify` 事实或 `LIST` 的时间戳/权限文本(本地浏览不受影响,仍显示真实数据) |
| 服务端无 `ABOR` 支持 | 客户端取消传输时,通过直接关闭数据连接来触发服务端的错误/结束路径,不是标准的 Telnet 带外 `ABOR` 中断——这个简化在实现中曾经引发一个真实 bug(见第 9.3 节),已经在客户端侧修复 |
| 上传时服务端不知道总大小 | `STOR` 没有客户端预先声明文件大小的机制,服务端只能在传输过程中报告"已收到多少字节",不能报告"总共多少字节/百分比"——服务端的传输进度回调对上传方向固定报 `total=0`(约定俗成的"未知"值) |

---

## 8. 版本历史与已完成里程碑

| 版本/阶段 | 关键内容 |
|---|---|
| v0.1.0 | 首个可用版本:自定义协议客户端+服务端完整实现,断点续传、失败重试、完整性校验、GitHub Actions 三平台发布流程 |
| v0.1.1 | 修复应用图标缺失、macOS Gatekeeper "已损坏"提示(临时签名) |
| v0.1.2 | 修复大文件(1.7GB+)传输中途断线(根因:UI 每个数据块都触发一次表格重绘,阻塞主线程导致心跳误超时)、进度条渲染污染相邻列、站点管理器"选中了 A 却连到 B"的过期状态 bug |
| v0.1.3 | 修复进度条整体错位到"方向"列(根因:`QStyleOptionProgressBar` 未初始化 `state`/`palette`/`direction`/`fontMetrics`,导致 macOS 原生控件绘制异常);同期发布了尚未接入 GUI 的新架构核心代码(阶段 A+B+C) |
| 阶段 D/E | `core::FtpTransferManager` 客户端并发传输池;过程中发现并修复了一个真实的协议层 bug(取消上传后续传会导致控制连接错位,详见第 9.3 节) |
| 阶段 F | 服务端结构化传输回调(供 GUI 展示实时状态)+ 局域网自动发现模块 |
| 阶段 G | GUI 正式接入新核心:`Connection`/`TransferQueue`/`FTPServer` 内部实现重写,公开接口基本不变 |
| 阶段 H | 端到端验证、删除旧协议代码(`common/protocol.*`、`server/session.*`、`server/datachannelserver.*`、`server/filemanager.*`)、文档更新(本次) |

---

## 9. 架构迁移完整记录

### 9.1 分阶段清单(全部完成)

| 阶段 | 内容 | 状态 |
|---|---|---|
| 阶段 A | `core/platform/socket.*`/`udp_socket.*`(跨平台原生 socket RAII 封装)、`core/heartbeat.*`(协议无关心跳线程)、`core/hash/sha256.*`(自实现,无外部依赖)、`core/callbacks.h` | ✅ |
| 阶段 B | `core/ftp/ftp_client.*` —— 真实 FTP 客户端状态机,同步阻塞式 API | ✅ |
| 阶段 C | `core/ftp/ftp_server.*` + `ftp_session.*` —— accept 线程 + 每客户端一线程模型,路径安全独立模块 `ftp_paths.*` | ✅ |
| 阶段 D/E | `core::FtpTransferManager` —— 客户端并发传输池(worker 池 + 任务队列 + 暂停/续传/重试) | ✅ |
| 阶段 F | 服务端结构化传输回调(`TransferStartedCallback`/`TransferProgressCallback`/`TransferCompletedCallback`)+ `core::DiscoveryBeacon`/`DiscoveryListener` 局域网发现 | ✅ |
| 阶段 G | GUI 接入:重写 `client/connection.*`、`client/transfer.*`、`server/ftpserver.*` 内部实现;`server/serverwindow.*`、`client/sitemanager.*` 接入局域网发现 UI | ✅ |
| 阶段 H | 端到端验证(headless + 手动)、删除旧协议代码、更新文档(本次) | ✅ |

### 9.2 验证方式

遵循"每阶段独立编译、真实运行验证,不是写完代码就算数"的原则,全程没有用 mock:

- **`core/` 层**(Qt-free):7 个独立测试可执行文件,均实测确认不链接任何 Qt 模块(`otool -L` 核实,只链接 `libc++`/`libSystem`)——`socket_smoke_test`、`ftp_client_parse_test`、`ftp_client_test`、`ftp_paths_test`、`ftp_server_test`(真实 `FtpServer` 被真实 `FtpClient` 通过真实回环 TCP 驱动,覆盖登录、目录操作、上传下载字节级校验、`REST` 续传、重命名、删除)、`ftp_transfer_manager_test`(并发上传下载、确定性暂停续传、失败自动重试耗尽、取消排队中的任务)、`lan_discovery_test`(真实广播+真实监听,发现/去重/过期移除)。
- **GUI-核心接线层**:`tests/gui_core_integration_test.cpp`,一个不依赖 `QWidget` 的 headless Qt 控制台程序,直接链接真实的 `client/connection.cpp`、`client/transfer.cpp`、`server/ftpserver.cpp`,驱动真实 `FTPServer` + `Connection` + `TransferQueue` 在 `127.0.0.1` 上真实互通——延续本项目一贯的验证方法论。覆盖连接/登录/列目录/建目录、上传下载的字节级 SHA-256 校验、以及**通过 `TransferQueue` 这一层**(不是直接调底层)的暂停/续传/取消,一次性通过全部 24 项断言。
- **真实 GUI 点击测试**:过程中反复触发了一个已知的、和这次改动无关的 Qt 6.8 + macOS 无障碍功能框架崩溃(详见 CLAUDE.md),导致纯点击式验证在这次收尾阶段做得不完整;改为以上面的 headless 集成测试作为主要验证手段,它跑的是完全相同的生产代码,不是 mock。
- **大文件复测**:临时把 headless 测试里的暂停测试文件从 100MB 调到 1.5GB,一次性手动验证完整上传+暂停+续传全过程在新协议栈下约 34 秒完成、无掉线、续传后内容 SHA-256 精确匹配,复测通过后改回 100MB(保持日常测试套件运行快)。

### 9.3 迁移过程中发现并修复的真实 bug

- **进度条错位到"方向"列**(v0.1.3):`QStyleOptionProgressBar` 未初始化 `state`/`palette`/`direction`/`fontMetrics`,macOS 原生风格绘制控件时用了错误/过期的几何信息。
- **暂停上传后续传失败**(阶段 D/E):`FtpClient` 取消传输时发送 `ABOR` 并只读一条回复,默认那就是 `ABOR` 的回复——但服务端没有实现 `ABOR`,客户端关闭数据连接后,服务端的 `STOR` 循环会把这个 EOF 当作正常完成,直接回复 `226`(在服务端读到 `ABOR` 文本、判定为未知命令之前)。于是客户端把 `STOR` 自己的完成回复当成了 `ABOR` 的回复,真正的 `ABOR` 回复(502)留在控制连接里没被读走,导致这条连接后续所有命令的回复解析全部错位。修复方式:取消时先把原命令的完成回复读掉,再发 `ABOR` 探测,`ABOR` 自己的回复按 best-effort 读取,超时/失败不当错误处理。

### 9.4 SFTP 排除的决策记录

用户明确要求:"SFTP先不做,放弃最难的,先将其它的功能都实现了"。评估过的理由:需要引入 SSH/加密库(libssh2 + OpenSSL/mbedTLS 之类),三平台 CI 集成本身就是一块工程量;libssh2 没有现成的跨平台预编译包;只能做"客户端连别人的 SFTP 服务器",LanDrop 自己没法变成 SFTP 服务端(那是操作系统 sshd 的职责)。相比之下,GUI 接入新核心和局域网自动发现都是已有设计、工程量可控的工作,优先完成。

---

## 10. 测试与验收标准

### 10.1 验证方法论

| 验证层 | 方法 |
|---|---|
| `core/` 逻辑 | Qt-free 独立测试可执行文件,驱动真实 socket/服务端/客户端在真实回环网络上互通,不用 mock |
| GUI-核心接线 | headless Qt 控制台程序,直接链接真实 GUI 层源码(不是重新实现一遍),驱动真实对象互通 |
| GUI 交互 | macOS 上 `osascript`/System Events 脚本化点击真实界面(受限于第 11 节提到的已知工具链问题) |
| 发布流程 | 真实触发 GitHub Actions、下载真实构建产物、用 `otool -L`/`codesign -dv`/`spctl --assess` 核实产物本身 |
| 大文件/长时间传输 | 用真实的 GB 级文件实测 |

### 10.2 验收标准(全部达到)

- [x] 两台不同平台设备(如 Mac + Windows)之间可稳定互传大文件(GB 级)不掉线
- [x] 传输过程有可视化进度、速度、剩余时间
- [x] 断点续传、失败自动重试、并发传输正常工作
- [x] 三平台(macOS/Windows/Linux)构建产物可正常下载运行
- [x] GUI 完全接入新核心,旧协议代码已删除
- [x] 局域网自动发现设备可用(服务端广播、客户端监听并填表)
- [x] 现有全部用户可感知功能(断点续传、并发传输、进度可视化等)在新架构下行为不变或更优(除第 7.2 节列出的已知规格差异外)

尚待真实环境验证(受限于当前开发/测试环境,不是设计缺陷):

- [ ] 连接真实第三方标准 FTP 服务器(如群晖/威联通 NAS)——协议实现层面已支持,但只在 LanDrop 自己的客户端↔服务端之间做过实测,没有针对第三方服务器的实现差异做过兼容性测试

---

## 11. 已知限制与技术债

| 项 | 说明 |
|---|---|
| 未购买代码签名证书 | macOS/Windows 首次运行会被 Gatekeeper/SmartScreen 拦截,需要用户手动信任,README 已提供绕过步骤 |
| 无正式单元测试框架 | 用手写的 headless 集成程序 + 断言输出做验证,没有接入 GTest/Catch2 等标准测试框架 |
| 记住密码为明文存储 | 客户端"记住密码"选项存的是本机明文(QSettings),UI 上已提示仅建议个人设备使用,不是通用密码管理器级别的安全性 |
| 标准 FTP 无内建完整性校验 | 见第 7.2 节,协议标准化后必然的取舍,连接第三方服务器时会体现 |
| 服务端 `ABOR` 未实现 | 见第 7.2、9.3 节,客户端侧已经绕开了这个限制引发的连接错位问题,但服务端本身仍不支持标准的传输中断协议 |
| 远程文件浏览缺修改时间/权限 | 见第 7.2 节,`core::FtpClient` 目前不解析这两类信息 |
| macOS 特定的第三方 GUI 自动化崩溃 | 已知的 Qt 6.8 + macOS 无障碍功能框架 bug,在用脚本高强度模拟点击/查询时可能触发崩溃,和应用本身逻辑无关,真实用户鼠标操作不受影响;详见 CLAUDE.md |
| 未针对第三方 FTP 服务器做兼容性测试 | 见第 10.2 节 |
| SFTP 客户端不支持 | 明确排除,见第 5.6、9.4 节 |

---

## 12. 发布与运维流程

1. 开发者在本地完成功能开发 + 上述验证方法论跑通。
2. 提交代码到 `master` 分支。
3. 需要发布新版本时,打形如 `vX.Y.Z` 的 tag 并推送到 GitHub。
4. `.github/workflows/release.yml` 自动在 macOS(`macos-14`)/Windows/Linux 三个 runner 上编译、打包(macOS 用 `macdeployqt` + 临时代码签名 + `ditto` 压缩成 `.zip`;Windows 用 `windeployqt` 打包成 `.zip`;Linux 打包成 `.tar.gz`),并自动创建 GitHub Release、附上三平台安装包。
5. 也可以通过 `workflow_dispatch` 手动触发,只产出构建产物、不创建 Release,用于验证 CI 是否正常。

---

## 13. 附录:仓库目录结构说明

```
FTP_Soft/                      # 仓库根目录(注意:目录名历史遗留,品牌已是 LanDrop)
├── common/                    # 客户端/服务端共用的协议无关工具
│   ├── utils.*                 # 密码哈希、文件哈希、格式化
│   └── ftptypes.h               # FTP::FileInfo / FTP::DEFAULT_PORT(GUI 层仍需要)
├── core/                      # Qt-free 核心:标准 FTP(RFC 959)引擎
│   ├── platform/socket.*, udp_socket.*  # 原生 TCP/UDP socket 封装
│   ├── heartbeat.*             # 心跳线程
│   ├── hash/sha256.*           # 自实现 SHA-256
│   ├── callbacks.h             # std::function 回调接口
│   ├── ftp/ftp_client.*        # 标准 FTP 客户端状态机
│   ├── ftp/ftp_server.*        # 标准 FTP 服务端
│   ├── ftp/ftp_session.*       # 服务端会话处理
│   ├── ftp/ftp_paths.*         # 路径安全
│   ├── ftp/ftp_transfer_manager.*  # 客户端并发传输池
│   ├── discovery/lan_discovery.*   # 局域网自动发现
│   └── tests/                  # 7 个 Qt-free 独立验证程序
├── server/                    # 服务端 GUI:FTPServer(薄包装)/ServerWindow
├── client/                    # 客户端 GUI:Connection/TransferQueue(薄包装)/
│                               #   FileBrowserPanel/TransferWidget/MainWindow/SiteManagerDialog
├── tests/                     # Qt 依赖的 headless 集成测试(gui_core_integration_test)
├── resources/                 # 图标等资源
├── .github/workflows/         # CI/CD(release.yml)
├── CMakeLists.txt
└── README.md
```

---

## 14. 结语

架构迁移(自定义协议 → 标准 FTP,核心与 UI 分离)已经**全部完成**:GUI 完全接入新核心,旧协议代码已删除,局域网自动发现和客户端并发传输池这两项当初讨论过的能力也已经实现。SFTP 客户端支持经过评估后明确排除。当前主要的已知限制是标准 FTP 协议标准化带来的必然取舍(无内建完整性校验、无 `ABOR`、远程文件时间/权限信息缺失)——这些都是提前告知过的、可接受的产品行为变化,不是遗留 bug。下一步如果要继续,建议优先做的是:针对真实第三方 FTP 服务器(NAS)的兼容性验证,以及视需要决定是否要为 LanDrop 自己的服务端加一个非标准的完整性校验扩展命令。
