# LanDrop 项目需求书

| | |
|---|---|
| 文档版本 | v1.0 |
| 对应代码版本 | v0.1.3(GitHub Release) |
| 更新日期 | 2026-08-02 |
| 仓库 | https://github.com/DavidRedVest/LanDrop |

> 说明:本项目从"需求 → 实现"是一边做一边细化的过程,并非先有完整需求书再开发。这份文档是在核心功能基本完成后,对已实现内容、正在进行的架构迁移、以及尚未实现部分的完整梳理,既可作为项目现状说明,也可作为后续继续开发(尤其是架构迁移收尾)的依据。

---

## 1. 项目概述

### 1.1 项目名称

**LanDrop**(代码仓库/构建产物历史上叫过 `FTP_Soft`,现在只有品牌名改了,可执行文件、窗口标题、配置项都已统一为 LanDrop)。

### 1.2 一句话定位

一个跨平台(macOS / Windows / Linux)、客户端 + 服务端一体的局域网文件传输工具,界面参考 FileZilla / UltraFTP 的双栏浏览 + 传输队列模式,用 Qt6 + C++17 编写。

### 1.3 要解决的问题

用户在自己的多台设备之间(典型场景:一台 Mac + 一台 Windows 笔记本)传文件时,常见的选项都不够好:

- U 盘拷贝:慢,还要来回插拔;
- 第三方网盘/云同步:依赖公网、大文件慢、隐私顾虑、不稳定;
- 系统自带的隔空投送 / 网络共享:平台绑定(如 AirDrop 只能苹果生态内)、配置繁琐、大文件容易断。

LanDrop 的目标是:**在同一局域网内,双击打开就能用,大文件传输稳定不掉线,进度可视化,不需要额外账号或公网依赖。**

### 1.4 目标用户

- 拥有多台设备(尤其是跨平台,如 Mac + Windows)的个人开发者/技术爱好者;
- 需要稳定传输较大文件(GB 级)且希望有可视化进度、断点续传的场景;
- 不需要企业级多用户权限体系,只需要基本的用户名/密码认证。

---

## 2. 项目背景

项目最初以 `FTP_Soft` 为名启动,定位是"仿 FileZilla/UltraFTP 的局域网文件传输工具"。开发过程中经历了几个关键节点:

1. **Phase 1 落地**:实现了一套基于 Qt(`QTcpSocket`)的客户端/服务端,用自定义二进制协议(非标准 FTP)完成局域网内两台自有设备间的文件互传,包含断点续传、失败重试、完整性校验等核心能力。
2. **品牌化**:项目更名为 LanDrop,窗口标题、配置项、图标全部统一。
3. **发布工程化**:接入 GitHub Actions,实现 macOS/Windows/Linux 三平台自动构建 + GitHub Release 发布,解决了 macOS Gatekeeper 拦截、代码签名等实际分发问题。
4. **真实使用发现的 Bug 修复**:大文件(1.7GB+)传输中途断线、进度条渲染错位、站点管理器连错站点等问题,均通过真实场景复现后修复。
5. **架构重构启动(进行中)**:应用户明确要求,项目正在从"Qt 耦合的自定义协议"迁移到"核心逻辑(网络/并发)与 UI 彻底分离、协议改为标准 FTP(RFC 959)"的新架构,以便将来能连接任意标准 FTP 服务器(如 NAS),不再局限于"两台都装了本软件的设备"。这部分**是本需求书里唯一"设计已完成、尚未全部实现"的部分**,详见第 6、9 节。

---

## 3. 总体目标与设计原则

| 目标 | 说明 |
|---|---|
| 稳定优先 | 大文件、长时间传输不能掉线;所有关键 bug 必须用真实场景复现后再修,不能只凭代码走查 |
| 跨平台一致 | macOS / Windows / Linux 三端功能、界面、操作方式一致 |
| 开箱即用 | 发布物是解压即用的可执行文件,不需要安装包、不需要额外装 Qt(macOS/Windows 已打包运行库) |
| 核心与界面解耦(迁移中) | 网络通信、并发模型用标准 C++ + 原生 OS API 实现,不依赖 Qt;Qt 只负责界面 |
| 协议标准化(迁移中) | 从自定义二进制协议迁移到标准 FTP(RFC 959),具备连接第三方 FTP 服务器的能力 |
| 安全默认值 | 密码加盐哈希存储、路径穿越防护、未配置认证时默认拒绝而不是放行 |

---

## 4. 系统架构

### 4.1 现状:两套并存的实现

项目目前存在**两条技术路线并存**的状态,这是架构迁移期间的正常中间状态,不是遗留混乱:

```
┌─────────────────────────────────────────────────────────┐
│  路线 A(已发布使用,v0.1.0 ~ v0.1.3):                     │
│  Qt 耦合 + 自定义二进制协议                                 │
│  common/ (protocol.*, utils.*)                             │
│  server/ (ftpserver, session, filemanager, datachannelserver)│
│  client/ (connection, transfer, filebrowser, mainwindow...) │
│  —— GUI 目前实际连接、实际发布使用的就是这一套              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  路线 B(架构迁移中,core/ 下的新实现):                     │
│  Qt-free 核心 + 标准 FTP(RFC 959)                          │
│  core/platform/socket.*   —— 原生 socket 封装(阶段 A,已完成) │
│  core/heartbeat.*         —— 心跳线程(阶段 A,已完成)         │
│  core/hash/sha256.*       —— 自实现 SHA-256(阶段 A,已完成)   │
│  core/callbacks.h         —— std::function 回调接口(阶段 A)  │
│  core/ftp/ftp_client.*    —— FTP 客户端状态机(阶段 B,已完成) │
│  core/ftp/ftp_server.*    —— FTP 服务端(阶段 C,已完成)       │
│  core/ftp/ftp_session.*   —— 服务端会话(阶段 C,已完成)       │
│  core/ftp/ftp_paths.*     —— 路径安全(阶段 C,已完成)         │
│  —— 目前完全独立于 GUI 存在,尚未接入 client/、server/ 的界面 │
└─────────────────────────────────────────────────────────┘
```

路线 B 已经过独立验证(见第 9.2 节),但**尚未接入 GUI**,所以用户目前实际下载运行的 LanDrop 客户端/服务端,走的还是路线 A。这是有意为之——每个阶段独立编译验证,不在中途留下一个两边都不完整的状态(详见第 9 节的分阶段计划)。

### 4.2 路线 A 架构(当前实际运行的版本)

- `common/` — 客户端和服务端共用的协议编解码(`protocol.h/.cpp`)和工具函数(`utils.h/.cpp`,含路径校验、密码哈希、文件哈希、格式化)。
- `server/`
  - `FTPServer` — 管理所有客户端连接和用户账号,监听控制端口(默认 2121)和数据端口(控制端口+1)。
  - `Session` — 单个客户端的控制通道:登录、浏览、文件管理、发起传输请求。
  - `FileManager` — 唯一允许触碰服务端文件系统的模块,所有路径先经过校验防止目录穿越。
  - `DataChannelServer` — 实际文件字节收发,并发数可控(默认 4)。
  - `ServerWindow` — 服务端 GUI。
- `client/`
  - `Connection` — 控制通道封装,含心跳(PING/PONG,15 秒一次)。
  - `TransferQueue` — 传输队列引擎:限并发(默认 3)、失败自动重试(带退避)、断点续传。
  - `FileBrowserPanel` — 本地/远程双栏文件浏览。
  - `TransferWidget` — 传输队列可视化(进度条、速度、ETA、状态)。
  - `SiteManagerDialog` — 保存的连接站点管理。
  - `MainWindow` — 主界面。

**通信模型**:控制通道(登录/浏览/文件管理)+ 数据通道(实际字节传输)分离,类似经典 FTP 思路,但用的是自定义二进制帧格式(4 字节魔数 + 1 字节命令 + payload),不是标准 FTP 协议。

### 4.3 路线 B 架构(迁移目标,详见第 9 节进度)

- **核心原则**:网络通信与并发模型用原生 OS Socket API(`winsock2`/BSD socket)+ 标准 C++(`std::thread`/`std::mutex`/`std::condition_variable`/`std::atomic`)实现,完全不依赖 Qt;Qt 仅用于界面层,通过 `std::function` 回调 + `QMetaObject::invokeMethod(..., Qt::QueuedConnection)` 桥接。
- **协议**:标准 FTP(RFC 959),而不是自定义协议——这样将来能连接任意第三方标准 FTP 服务器(比如 NAS),不局限于两台都装了 LanDrop 的设备。
- 详细的分阶段实现内容和验证方式见第 9 节。

---

## 5. 功能需求

### 5.1 客户端功能(已实现,路线 A)

| 功能 | 说明 |
|---|---|
| 连接管理 | 站点管理器保存主机/端口/用户名/密码,一键连接;工具栏可见的"连接"/"断开连接"按钮 |
| 双栏文件浏览 | 左本地、右远程,同一套控件和交互:导航目录、新建文件夹、重命名、删除 |
| 文件上传/下载 | 选中文件后可发起上传到远程当前目录 / 下载到本地当前目录 |
| 传输队列可视化 | 进度条、传输速度、剩余时间(ETA)、状态(排队中/连接中/传输中/已完成/失败/已暂停/已取消) |
| 传输控制 | 暂停 / 继续 / 取消 / 从队列删除 / 清除已完成 |
| 断点续传 | 传输中断后重新发起,自动从已传输字节位置续传 |
| 失败自动重试 | 网络抖动导致失败会自动退避重试,超过次数上限后停止、可手动重试 |
| 记住密码(可选) | 明文保存在本机 QSettings,UI 上明确提示"仅建议个人设备使用" |

### 5.2 服务端功能(已实现,路线 A)

| 功能 | 说明 |
|---|---|
| 服务配置 | 指定根目录、控制端口(数据端口自动为控制端口+1)、启动/停止服务 |
| 用户管理 | 添加/删除用户(用户名+密码),密码加盐哈希存储,不落地明文 |
| 会话可视化 | 实时显示当前连接会话数、连接/断开日志、传输日志 |
| 并发控制 | 数据通道并发数可控(默认 4) |
| 路径安全 | 所有客户端请求路径经过校验和钳制,禁止目录穿越到根目录之外 |

### 5.3 传输引擎功能需求(核心,已实现)

| 需求 | 说明 |
|---|---|
| 大文件稳定性 | 需支持 GB 级文件传输全程不因 UI 刷新或心跳超时而误断线(已通过 1.8GB 实测验证) |
| 完整性校验 | 接收方对完整文件计算 SHA-256,和发送方在完成包里带的哈希比对;失败时(路线 A)服务端自动删除损坏文件,要求重传 |
| 分块传输 | 数据读写必须用固定大小内存块,不能整文件读入内存 |
| 心跳保活 | 长时间传输时,控制通道需要独立机制防止被 NAT/防火墙判定超时挂断 |

### 5.4 跨平台打包与分发需求(已实现)

| 需求 | 说明 |
|---|---|
| 三平台构建 | macOS(`.app` + `macdeployqt`)、Windows(`windeployqt`)、Linux 二进制,均由 GitHub Actions 自动构建 |
| 免安装分发 | 解压即用,不需要安装向导,不额外写入系统 |
| 自动发布 | 推送 `v*.*.*` 格式的 tag 自动触发构建并创建 GitHub Release,附带各平台压缩包 |
| Gatekeeper/SmartScreen 说明 | 由于没有付费代码签名证书,README 中需明确写清楚绕过步骤 |

### 5.5 安全需求

| 需求 | 说明 |
|---|---|
| 密码存储 | 服务端密码加盐 SHA-256 哈希存储,任何时候不比较/存储明文 |
| 路径穿越防护 | 所有客户端提交的路径必须经过规范化+根目录钳制校验 |
| 认证默认拒绝 | (路线 B 新增设计)未配置认证回调时,任何登录一律拒绝,而不是默认放行 |

### 5.6 路线 B(标准 FTP 迁移)新增功能需求

| 需求 | 状态 |
|---|---|
| 原生 socket 封装(跨平台) | ✅ 已完成(阶段 A) |
| 独立心跳线程 | ✅ 已完成(阶段 A) |
| 无第三方依赖 SHA-256 | ✅ 已完成(阶段 A) |
| 标准 FTP 客户端状态机(USER/PASS/PWD/CWD/PASV/LIST/MLSD/RETR/STOR/REST/DELE/MKD/RMD/RNFR+RNTO/TYPE/NOOP/QUIT) | ✅ 已完成(阶段 B) |
| 标准 FTP 服务端(accept 线程 + 每客户端一线程,PASV 动态端口分配) | ✅ 已完成(阶段 C) |
| GUI 接入新核心 | ❌ 未开始(阶段 D) |
| 连接第三方标准 FTP 服务器(如 NAS) | ❌ 依赖阶段 D 完成 |
| SFTP 客户端支持 | ❌ 未规划实现(仅在早期讨论中提及,不在当前范围) |
| 局域网自动发现设备 | ❌ 未规划实现 |

---

## 6. 非功能需求

| 类别 | 需求 |
|---|---|
| 性能 | 局域网内大文件传输速度应接近网卡/磁盘 IO 上限(实测 1.8GB 文件约 80MB/s,22 秒完成) |
| 稳定性 | 长时间/大文件传输不能因 UI 层原因(如高频重绘)拖慢主线程导致心跳误超时 |
| 兼容性 | macOS(Intel/Apple Silicon)、Windows、Linux 三端一致行为 |
| 可维护性 | 核心网络逻辑与 UI 逻辑分离(路线 B 目标),便于独立测试和替换 |
| 可测试性 | 关键逻辑需要能在无 GUI 环境下自动化验证(见第 10 节) |
| 安全性 | 密码不落地明文;路径访问严格限制在授权根目录内 |

---

## 7. 协议设计

### 7.1 现行自定义协议(路线 A)

- **帧结构**:`Packet` = 4 字节魔数(`0x46545001`)+ 1 字节 `Command` + `QByteArray` payload(`QDataStream` 编码)。
- **流式分帧**:`writeFramedPacket()` 在序列化后的包前加 `quint32` 长度前缀;`PacketFramer` 在接收端缓冲字节,凑够一个完整帧才产出一个 `Packet`。
- **双通道模型**:一条长连接控制通道(默认端口 2121)负责登录/浏览/文件管理;每次实际传输另开一条短连接数据通道(端口 = 控制端口 + 1)。传输请求通过控制通道换取一次性 token + 续传偏移量,数据通道握手时提交这个 token。
- **续传**:数据包携带绝对字节偏移,接收端按偏移 seek 后写入,重试/续传天然幂等。
- **完整性校验**:接收方对收到的完整文件算 SHA-256,和发送方在完成包里带的哈希比对。

### 7.2 迁移目标协议(路线 B,标准 FTP / RFC 959)

- 命令集:`USER`/`PASS`/`PWD`/`CWD`/`PASV`/`LIST`/`MLSD`/`RETR`/`STOR`/`REST`/`DELE`/`MKD`/`RMD`/`RNFR`+`RNTO`/`TYPE I`/`NOOP`/`QUIT`。
- 目录列表优先用 `MLSD`(机器可读,通过 `FEAT` 探测服务端是否支持),不支持时退回解析 `LIST` 的类 Unix `ls -l` 文本格式。
- **已知规格变化(不是遗漏,是标准化的必然取舍)**:标准 FTP 没有内建的"传输后自动校验哈希"机制。连接第三方 FTP 服务器时无法要求对方支持;连接 LanDrop 自己的新服务端时,计划通过 `FEAT` 能力发现网关一个非标准扩展命令,双方都支持时启用,不支持时降级为"不做应用层校验,只依赖 TCP 本身的可靠性"。

---

## 8. 版本历史与已完成里程碑

| 版本 | 关键内容 |
|---|---|
| v0.1.0 | 首个可用版本:路线 A 客户端+服务端完整实现,断点续传、失败重试、完整性校验、GitHub Actions 三平台发布流程 |
| v0.1.1 | 修复应用图标缺失、macOS Gatekeeper "已损坏"提示(临时签名) |
| v0.1.2 | 修复大文件(1.7GB+)传输中途断线(根因:UI 每个数据块都触发一次表格重绘,阻塞主线程导致心跳误超时)、进度条渲染污染相邻列、站点管理器"选中了 A 却连到 B"的过期状态 bug |
| v0.1.3 | 修复进度条整体错位到"方向"列(根因:`QStyleOptionProgressBar` 未初始化 `state`/`palette`/`direction`/`fontMetrics`,导致 macOS 原生控件绘制异常);同时随版本一起发布了尚未接入 GUI 的路线 B 核心代码(阶段 A+B+C,详见第 9 节) |

---

## 9. 架构迁移详细进度(路线 B)

### 9.1 分阶段计划

| 阶段 | 内容 | 状态 |
|---|---|---|
| 阶段 A | `core/platform/socket.*`(跨平台原生 socket RAII 封装)、`core/heartbeat.*`(协议无关心跳线程)、`core/hash/sha256.*`(自实现,无外部依赖)、`core/callbacks.h`(`std::function` 回调接口) | ✅ 已完成 |
| 阶段 B | `core/ftp/ftp_client.*` —— 真实 FTP 客户端状态机,同步阻塞式 API,内部心跳仅在空闲时发 `NOOP`(避免和传输中的响应交错) | ✅ 已完成 |
| 阶段 C | `core/ftp/ftp_server.*` + `ftp_session.*` —— accept 线程 + 每客户端一线程模型,`PASV` 动态端口分配,路径安全独立模块 `ftp_paths.*` | ✅ 已完成 |
| 阶段 D | Qt 适配层(`client/ftpclientadapter.*`、`server/ftpserveradapter.*`),把回调转成 Qt 信号,重新接线现有 GUI(`MainWindow`/`FileBrowserPanel`/`TransferWidget`/`SiteManagerDialog`/`ServerWindow` 保留,只改底层调用),验证通过后删除路线 A 的旧协议代码(`common/protocol.*`、`client/connection.*`、`client/transfer.*` 里的 `ClientTransferWorker`、`server/session.*`、`server/datachannelserver.*`、`server/ftpserver.*`) | ❌ **未开始,是当前最主要的剩余工作** |

### 9.2 阶段 A/B/C 的验证方式(已完成部分)

遵循"每阶段独立编译、真实运行验证,不是写完代码就算数"的原则:

- 新增 CMake 静态库 target `ftpcore`,不链接任何 Qt 模块,`AUTOMOC`/`AUTORCC`/`AUTOUIC` 显式关闭,并用 `otool -L` 实测确认最终二进制只链接 `libc++`/`libSystem`(Qt-free 是硬性验证过的,不是口头声明)。
- **5 个独立测试可执行文件**,共 96+ 项断言,全部通过:
  - `socket_smoke_test` —— socket 回环收发、超时行为、心跳线程启停、SHA-256 已知向量比对
  - `ftp_client_parse_test` —— PASV 元组解析、MLSD/Unix LIST 目录解析(含符号链接、`.`/`..` 跳过)
  - `ftp_client_test` —— 用脚本化的假 FTP 服务器,验证真实 `FtpClient` 的登录/PWD/CWD 状态机
  - `ftp_paths_test` —— 路径安全模块的回归测试,专门覆盖旧代码真实出现过的"忘记去掉前导斜杠"类 bug 和路径穿越攻击
  - `ftp_server_test` —— **真正的端到端集成测试**:真实 `FtpServer`(根目录是磁盘上的真实临时目录)被真实 `FtpClient` 通过真实回环 TCP 驱动,覆盖错误密码拒绝、登录、目录操作、上传+下载字节级校验、`REST` 续传下载、重命名、删除、清空服务

### 9.3 阶段 D 的剩余工作范围(供后续排期参考)

1. 设计并实现 `client/ftpclientadapter.h/.cpp`:把 `core::FtpClient` 的 `std::function` 回调(进度/状态/错误)转成 Qt 信号,内部用独立 `std::thread` 跑阻塞式的 `FtpClient` 调用,通过 `QMetaObject::invokeMethod(..., Qt::QueuedConnection)` 安全地把结果甩回 UI 线程(镜像现有 `ClientTransferWorker`/`QThread` 的既有模式,只是把 `QThread` 换成 `std::thread`)。
2. 同理设计 `server/ftpserveradapter.h/.cpp`,把 `core::FtpServer` 的认证回调对接到 `ServerWindow` 现有的 `QSettings` 用户存储(`FtpServer::Authenticator` 的接口已经特意设计成和旧 `Session::Authenticator` 同构,方便直接复用)。
3. 把 `MainWindow`/`FileBrowserPanel`/`TransferWidget`/`SiteManagerDialog`/`ServerWindow` 里对旧 `Connection`/`TransferQueue`/`FTPServer` 的直接调用,逐个改接到新适配层(界面本身基本不用大改,主要是底层调用替换)。
4. 端到端验证通过、确认新路径能完全替代旧路径的所有现有功能(浏览、上传下载、断点续传、并发传输、UI 进度显示)之后,再删除路线 A 的旧代码,避免中途出现"两边都不完整"的状态。
5. **需要向用户重新确认**:标准 FTP 协议本身没有内建完整性校验,阶段 D 完成后,"自动 SHA-256 校验" 这个用户可感知的行为,在连接第三方 FTP 服务器时会消失(连接 LanDrop 自己的新服务端时可以通过非标准扩展保留,见第 7.2 节)。这是一个需要提前告知用户的产品行为变化,不是回归 bug。

---

## 10. 测试与验收标准

项目采用的验证方法论(不是"能编译就行",是"真实场景跑过才算数"):

| 验证层 | 方法 |
|---|---|
| 协议/传输引擎逻辑 | 写无 GUI 的 headless 集成测试程序,直接链接协议/传输相关代码,驱动真实 `FTPServer`/`Connection`/`TransferQueue`(路线 A)或 `FtpServer`/`FtpClient`(路线 B)在 `127.0.0.1` 上真实互通,而不是 mock |
| GUI 交互 | macOS 上用 Accessibility API(`osascript`/System Events)脚本化点击真实界面,曾经用这种方式抓到过两个代码走查完全发现不了的真实 bug(连接按钮只在菜单栏可见、密码重复提示逻辑错误) |
| 发布流程 | 真实触发 GitHub Actions、下载真实构建产物、用 `otool -L`/`codesign -dv`/`spctl --assess` 等工具核实产物本身的正确性,不只是看 CI 显示"成功" |
| 大文件/长时间传输 | 用真实的 GB 级文件实测(如 1.8GB 文件传输无掉线) |

**验收标准**(路线 A,当前已达到):

- [x] 两台不同平台设备(如 Mac + Windows)之间可稳定互传大文件(GB 级)不掉线
- [x] 传输过程有可视化进度、速度、剩余时间
- [x] 断点续传、失败自动重试正常工作
- [x] 完整性校验能正确检测并处理传输错误
- [x] 三平台(macOS/Windows/Linux)构建产物可正常下载运行

**验收标准**(路线 B,阶段 D 完成后适用,当前未达到):

- [ ] GUI 完全接入新核心,旧协议代码已删除
- [ ] 能连接第三方标准 FTP 服务器(如群晖/威联通 NAS)并正常浏览、上传、下载
- [ ] 现有全部用户可感知功能(断点续传、并发传输、进度可视化等)在新架构下行为不变或更优

---

## 11. 已知限制与技术债

| 项 | 说明 |
|---|---|
| 未购买代码签名证书 | macOS/Windows 首次运行会被 Gatekeeper/SmartScreen 拦截,需要用户手动信任,README 已提供绕过步骤 |
| 无正式单元测试框架 | 目前用手写的 headless 集成程序 + 断言输出做验证,没有接入 GTest/Catch2 等标准测试框架 |
| 记住密码为明文存储 | 客户端"记住密码"选项存的是本机明文(QSettings),UI 上已提示仅建议个人设备使用,不是通用密码管理器级别的安全性 |
| 标准 FTP 无内建完整性校验 | 见第 7.2、9.3 节,是协议标准化后必然的取舍,阶段 D 完成、连接第三方服务器时会体现 |
| 服务端 `ABOR` 未实现 | 路线 B 的新服务端(阶段 C)目前没有支持传输中途被动取消(`ABOR`),客户端侧取消时通过关闭数据连接来触发服务端的错误路径,不是标准的协议级中断 |
| macOS 特定的第三方 GUI 自动化崩溃 | 已知的 Qt 6.8 + macOS 无障碍功能框架 bug,在用脚本高强度模拟点击时可能触发崩溃,和应用本身逻辑无关,真实用户鼠标操作不受影响 |

---

## 12. 发布与运维流程

1. 开发者在本地完成功能开发 + 上述验证方法论跑通。
2. 提交代码到 `master` 分支。
3. 需要发布新版本时,打形如 `vX.Y.Z` 的 tag 并推送到 GitHub。
4. `.github/workflows/release.yml` 自动在 macOS(`macos-14`)/Windows/Linux 三个 runner 上编译、打包(macOS 用 `macdeployqt` + 临时代码签名 + `ditto` 压缩成 `.zip`;Windows 用 `windeployqt` 打包成 `.zip`;Linux 打包成 `.tar.gz`),并自动创建 GitHub Release、附上三平台安装包。
5. 也可以通过 `workflow_dispatch` 手动触发,只产出构建产物、不创建 Release,用于验证 CI 是否正常。

---

## 13. 附录:仓库目录结构说明

```
FTP_Soft/                      # 仓库根目录(注意:目录名历史遗留,品牌已是 LanDrop)
├── common/                    # 客户端/服务端共用:协议编解码 + 工具函数(路线 A)
├── server/                    # 服务端:FTPServer/Session/FileManager/DataChannelServer/ServerWindow(路线 A)
├── client/                    # 客户端:Connection/TransferQueue/FileBrowserPanel/TransferWidget/MainWindow/SiteManagerDialog(路线 A)
├── core/                      # Qt-free 新核心(路线 B,阶段 A+B+C 已完成,未接入 GUI)
│   ├── platform/socket.*      # 原生 socket 封装
│   ├── heartbeat.*            # 心跳线程
│   ├── hash/sha256.*          # 自实现 SHA-256
│   ├── callbacks.h            # std::function 回调接口
│   ├── ftp/ftp_client.*       # 标准 FTP 客户端状态机
│   ├── ftp/ftp_server.*       # 标准 FTP 服务端
│   ├── ftp/ftp_session.*      # 服务端会话处理
│   ├── ftp/ftp_paths.*        # 路径安全
│   └── tests/                 # 5 个独立验证程序
├── resources/                 # 图标等资源
├── .github/workflows/         # CI/CD(release.yml)
├── CMakeLists.txt
└── README.md
```

---

## 14. 结语

第一阶段(路线 A,自有协议、局域网互传)的目标已经达成并稳定发布到 v0.1.3。项目当前最主要的、有明确设计但尚未实现的工作是**架构迁移的阶段 D**——把已经独立验证过的标准 FTP 核心(路线 B)接入现有 GUI,并在验证通过后清理掉旧协议代码。除此之外,局域网自动发现设备等更早期讨论过的方向目前不在实现范围内。
